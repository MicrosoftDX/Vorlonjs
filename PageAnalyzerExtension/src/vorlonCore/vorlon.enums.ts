module VORLON {
    export enum RuntimeSide {
        Client,
        Dashboard,
        Both
    }

    export enum PluginType {
        OneOne,
        MulticastReceiveOnly, 
        Multicast
    }
}
